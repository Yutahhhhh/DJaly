# 2026-09-06 ローカル配備・再解析記録

## 配備

- このMacの`/Applications/Djaly.app`をv0.4.1へ更新。GitHub公開リリース・アップロードは実施していない。
- 既存アプリを`/Applications/Djaly-0.4.0-backup-20260906.app`へ退避した。
- 停止中のDBを`/Users/horiyuuta/Library/Application Support/Djaly/backups/20260906-before-selective-analysis/djaly.duckdb`へバックアップした。
- 更新前DBとバックアップのSHA256は共に`09f199f2ce34ff0a7589c6f13042a736d99636c712863f6affe7c9dfb3eab252`。
- PyInstallerサイドカーとTauriアプリをビルドし、ローカル用署名の検証、インストール先からの起動、48123番ポートのAPI/MCP疎通を確認した。

## 実データでの試行

| ジョブ | 件数 | 並列数 | 完了時間 | エラー |
| --- | ---: | ---: | ---: | ---: |
| `318768c7e6644633bbf60e5d8efa03a0` | 30 | 2 | 97.85秒 | 0 |
| `41045e9bf36e47319706a12e4bf526bc` | 30 | 4 | 77.25秒 | 0 |

異なる30曲なので厳密な速度比較ではない。4並列時のシステムメモリ空き指標は45%で、各ワーカーの常駐メモリは約380〜410MBだった。
試行ではセットリストに使われている曲を優先し、`embedding`だけ更新した。新しい方式の60曲は次のジョブで繰り返さない。

60曲完了後にアプリを一旦停止し、バックアップと全DBを読み取り専用で照合した。以下の全件数・ハッシュ集計が一致した。

- 楽曲情報24,983件（BPM・キー・ジャンル等を含む）
- 歌詞3,720件
- セットリスト12件、構成曲227件
- ビート・波形24,983件

音源3曲（ID 201・227・242）の再解析前後のSHA256も一致した。全音源の内容ハッシュ照合までは行っていない。

## 音源の所在

24,983曲の登録先を調べ、24,638曲の音源を確認した。345曲は登録されたパスに通常ファイルが存在しなかった。
一覧はバックアップと同じフォルダの`audio-availability.json`に保存した。
見つからない音源の再リンク・タグ変更・削除はしていない。解析ジョブ内で個別の失敗として保持し、音源の復元後に再試行できる。

## 操作

本番全体ジョブを2026-09-06 02:58 JSTに開始した。

- ID：`0b605479b864465db22bc790a569015d`
- 対象：24,923曲（試行で完了した60曲を自動除外）
- 設定：`features=["embedding"]`、`only_outdated=true`、`workers=4`
- 事前に音源を確認できた未処理曲：24,578曲。所在不明の345曲はエラーとして記録し、他の曲の処理は継続する。
- 60秒おきの監視ログ：`/Users/horiyuuta/Library/Application Support/Djaly/analysis-0b605479b864465db22bc790a569015d.log`
- 監視プロセスに`caffeinate -i`を付け、監視中のアイドルスリープを防止。ジョブ終了・停止時に監視も終了する。Macの蓋を閉じるスリープや再起動は防止しない。
- 試行からの所要時間は十数時間〜20時間程度の暫定目安であり、完了を意味しない。最新状態はMCP/下記コマンドで確認する。

```sh
backend/.venv/bin/python scripts/track_analysis_job.py status --job-id 0b605479b864465db22bc790a569015d
backend/.venv/bin/python scripts/track_analysis_job.py pause --job-id 0b605479b864465db22bc790a569015d
backend/.venv/bin/python scripts/track_analysis_job.py resume --job-id 0b605479b864465db22bc790a569015d --workers 4
```

停止後に再開した場合は、必要に応じて`caffeinate -i ... track_analysis_job.py watch --job-id ...`も再実行する。

MCPの選択解析・中断再開方法は[選択解析の説明](selective-audio-analysis.md)を参照。
アプリを終了・Macを再起動した場合は、完了分を保持したまま一時停止状態となる。再起動後は`resume_track_analysis`で明示的に再開する。
移行中は新旧の類似度ベクトルを直接比較しないため、類似曲検索の対象が一時的に減る。

音楽的な正解率・セットリストの試聴採用率は未評価。ここでの成功は処理・データ保持の検証を意味する。

最終再実行でもバックエンド全187テストが通過（11.37秒、既存のPydantic非推奨警告1件）。TypeScript/Viteビルド、配布バイナリの実音源3曲・本番60曲の試行も成功した。

## 同日追加修正：解析中の検索接続競合（v0.4.2）

v0.4.1の稼働中、類似検索12リクエストを4並列で実行し、11件で`Unique file handle conflict`を再現した。
毎回接続を開閉する`NullPool`を、最大8接続・追加の一時接続なしの永続`QueuePool`へ変更した。最初の物理接続作成もロックで順番に行う。SQL実行そのものと4並列の音響推論は直列化しない。

回帰テストでは、実DBと同じJSON文字列保存・検索時のベクトル変換を使い、6スレッドで計240回の検索／更新を実行した。接続再利用、ロールバック後の再利用、長いセッション中の別接続による検索も確認し、全190テストが通過した。
解析アルゴリズム・モデル・バージョン判定・既存結果は変更しないため、この修正を理由とする再解析のやり直しは不要。

修正前の追加再現確認中、保存側にも同じ接続競合が発生し91曲が失敗として記録された（別に音源不明338曲）。一時停止時点は本番ジョブ完了16,080曲、試行込み16,140曲、未処理8,414曲、失敗429曲。
この時点のDBとジョブキューを`/Users/horiyuuta/Library/Application Support/Djaly/backups/20260906-before-db-connection-fix/`へ保存し、v0.4.1アプリを`/Applications/Djaly-0.4.1-backup-20260906.app`へ退避した。
再解析開始前のバックアップと改めて全件照合し、楽曲・歌詞・セットリスト・ビート・波形のハッシュ一致を確認した。

注意：ジョブキューの単純な本体ファイルコピー`analysis-jobs.sqlite3`はWAL内の最新状態を含まず、停止直前のキュー復元には使わない。再開後にSQLite Backup APIで整合した`analysis-jobs-consistent-after-resume.sqlite3`を別途作成し、`integrity_check=ok`を確認した。楽曲DBの停止時スナップショットはWALを畳み込み済みで、新方式16,140曲を含む。

v0.4.2へ更新後、同一ジョブを`retry_failed=true`で再開し、再解析中に類似検索36件を4並列で実行して全件成功（最遅1.805秒）。接続競合で未更新だった91曲は、停止中の楽曲DBでID 16403〜16493と確認した。

続けて同じ並列検索36件を再実行し全件成功（最遅1.563秒）。計72件のAPI検索と、実MCP経由の3曲の類似検索が成功した。
対象91曲の再試行は90曲が完了。残るID 16457は元の登録先に音源がなく、既存データを保持して保留とした。確認時点の全失敗339件はすべて音源不明で、接続競合の失敗は0件となった。
本番ジョブの完了数は16,176曲へ増加し（試行込み16,236曲）、未処理の続きを実行中。既存の進捗監視とアイドルスリープ防止も継続している。
